; <?php /*

[General]

; The subfolder path to the site, if it's not
; running in the root of a site.
path = "/minvc/"

; The default handler for homepage requests.
default_handler = "example/index"

; The 404 error handler if no others match.
404_handler = "example/404"

; The template rendering engine for views.
template_renderer = "minvc\Template"

; The timezone for the site.
timezone = GMT

; Whether to gzip output for browsers that support it.
; This usually gives a noticeable performance boost.
compress_output = On

; */ ?>